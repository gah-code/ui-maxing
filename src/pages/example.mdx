# Welcome to My Site

This is an example of how you can style elements in MDX content using **Theme UI**.

Here’s a code snippet:

````js
const greeting = "Hello, world!";
console.log(greeting);


```jsx
// Example code block
const greeting = 'Hello, World!';
console.log(greeting);

function handleDeleteTask(taskId) {
  dispatch(
    // "action" object:
    {
      type: 'deleted',
      id: taskId,
    }
  );
}
````
